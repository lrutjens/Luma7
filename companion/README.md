# Luma7 iOS Companion (planned)

Step 3 in DESIGN.md: BLE relay for away-from-home use, server configuration UI, and manual text controls.

Not implemented in this milestone. The server already exposes `POST /api/command` with `respond_in_body` for synchronous manual control when the companion app is built.
